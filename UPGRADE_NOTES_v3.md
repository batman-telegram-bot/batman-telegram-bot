# Gotham Bot — پک آپدیت دکمه‌ای (روی upgrade‌های قبلی)

## چی عوض شد

### 📁 ساختار پوشه‌ها
هر قابلیت حالا تو پوشه‌ی خودشه:
- `games/` — همه‌ی بازی‌ها (قبلاً games.py, games_pack2-5.py, board_games.py,
  ttt_inline.py, ttt_gotham.py جدا جدا تو ریشه بودن)
- `downloader/` — دانلودر اینستاگرام/یوتیوب/پینترست
- `admin/` — پنل مدیریت گروه جدید (دکمه‌ای)
- `bot.py` همون فایل اصلیه، فقط importها به مسیر جدید اشاره می‌کنن. هیچ منطقی
  عوض نشده، فقط جابه‌جا شده — تک تک importها دستی چک شدن که چرخه‌ای نشن.

### 🎮 منوی بازی‌ها با دکمه (games/menu.py)
بنویس «گیم» → یه منو مثل انتخاب شخصیت باز می‌شه: ۵ دسته (سریع، کلمه‌ای، دو
نفره، پازل، گروهی)، هر کدوم رو بزنی لیست بازی‌های اون دسته میاد، بازی رو بزن
شروع می‌شه. جمعاً ۳۵ بازی الان از طریق دکمه هم قابل‌دسترسن.
نوشتنِ اسم بازی هنوز هم دقیقاً مثل قبل کار می‌کنه — چیزی حذف نشده.

### 📥 دانلودر با یک دکمه
تو پنل تنظیمات (⚙️)، دکمه‌ی «📥 دانلودر» حالا مستقیم منوی انتخاب پلتفرم
(اینستاگرام/یوتیوب/پینترست) رو باز می‌کنه — دیگه لازم نیست اول بنویسی
«دانلودر». (نوشتنش هم هنوز کار می‌کنه.)

### 🛡 پنل مدیریت گروه، کاملاً دکمه‌ای (admin/panel.py)
روی پیام یه عضو ریپلای کن و بنویس «مدیریت» → یه منو باز می‌شه:
🔨 بن · 👢 کیک · 🔇 میوت (با انتخاب مدت: ۱۰ دقیقه/۱ ساعت/۱ روز/دائم) ·
🔊 آنمیوت · ⚠️ اخطار · ✅ حذف اخطار · ⭐ ویژه · 🛡 معاف · 🗑 حذف پیامش.
دیگه لازم نیست /ban یا /mute رو حفظ باشی. دستورهای اسلش قدیمی هم دست‌نخورده
باقی موندن، برای هرکی بهشون عادت داره.

⚠️ درباره‌ی نمونه‌کد Alita_Robot (github.com/divkix/Alita_Robot) که لینک
دادی: اون ربات با Pyrogram/aiogram نوشته شده، این ربات با
python-telegram-bot — دو فریم‌ورک متفاوت، پس کدش مستقیم قابل‌کپی نبود.
تجربه‌ی کاربریِ «دکمه به‌جای دستور»ش رو از صفر با فریم‌ورک خودِ این پروژه
بازسازی کردم.

## تست
- همه‌ی فایل‌ها با `py_compile` چک شدن (بدون خطای سینتکسی).
- کل زنجیره‌ی import (بدون شبکه/بدون توکن واقعی، با stub برای
  python-telegram-bot/httpx/yt-dlp/chess) اجرا شد و `bot.main()` تمام
  handlerها رو بدون خطا رجیستر کرد.
- چک شد که هیچ کلید بازی تکراری نیست و هیچ callback_data از سقف ۶۴ بایت
  تلگرام رد نمی‌شه (۳۵ بازی، صفر تداخل).
- ⚠️ چیزی که این‌جا قابل‌تست نبود: اجرای واقعی رو یه سرور با توکن واقعی و
  یه گروه واقعی (رفتار دقیق Telegram API — رتبه‌ی ادمین، پرمیشن‌ها و غیره).
  پیشنهاد می‌کنم اول رو یه گروه تستی امتحانش کنی، بعد رو گروه اصلی.

## فایل‌های تغییرکرده/جابه‌جاشده
- bot.py — importها به‌روز شد، پنل بازی‌ها/دانلودر دکمه‌ای شد، پنل مدیریت
  دکمه‌ای وصل شد
- games/menu.py — **جدید**
- admin/panel.py, admin/__init__.py — **جدید**
- games/*.py, downloader/__init__.py — جابه‌جا شدن به پوشه، بدون تغییر منطق
  (به‌جز یک import نسبی تو games/core.py)
