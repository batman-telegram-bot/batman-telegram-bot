# آپدیت v4 — رفع باگ پنل + یادآور واقعی + تشخیص فیلم/آهنگ + خلاصه‌ساز گروه

## باگ‌های رفع‌شده

1. **دکمه‌های «امکانات جدید» کار نمی‌کردن**: تو `button_handler` (bot.py)، `query.answer()`
   دوبار صدا زده می‌شد (یه‌بار بی‌قید-وشرط بالای تابع، یه‌بار دیگه برای دکمه‌های
   فال/اسلات/پرونده‌روز/کوییز/کپسول/شهروند‌نمونه/چرخ‌گردون/تیکت/تولد/صدا‌به‌متن).
   تلگرام فقط یه‌بار اجازه‌ی answer به هر callback query می‌ده، پس بار دوم خطا
   می‌داد و دکمه از دید کاربر «هیچ کاری نمی‌کرد». حالا این چک اول از همه انجام
   می‌شه و دیگه تکراری نیست.

2. **دانلودر بدون دکمه‌ی بازگشت**: به منوی انتخاب پلتفرم و به پیام «فلان پلتفرم
   انتخاب شد» دکمه‌ی بازگشت اضافه شد.

3. بخش «همه کلمات» از قبل دکمه داشت (`همه کلمات` / `کلمات ربات` → پیام با دکمه‌ی
   بازگشت)؛ چیزی برای رفع نبود.

## فیچرهای جدید

### ⏰ یادآور واقعی (`reminders.py`)
- `یادآور 10 دقیقه <متن>` / `یادآور 2 ساعت <متن>` / `یادآور 1 روز <متن>`
- `یادآور 14:30 <متن>` (امروز، یا اگه گذشته فردا)
- `یادآور فردا 9:00 <متن>`
- `یادآورهای من` — لیست یادآورهای فعال
- `حذف یادآور <شماره>`
- تو دیتابیس (`bot.db`) ذخیره می‌شه، پس بعد از ریستارت/دیپلوی جدید رو Railway هم گم نمی‌شه.

### 🎬 تشخیص فیلم/سریال + 🎵 تشخیص آهنگ + 📝 خلاصه‌ی گروه (`media_recognition.py`)
- ریپلای رو عکس/ویدیوی یه صحنه + «تشخیص فیلم» → با یه مدل چندوجهیِ رایگان
  (OpenRouter) حدس می‌زنه، بعد با TMDB تایید/پوستر می‌گیره.
- ریپلای رو ویس/صدا/ویدیو + «تشخیص آهنگ» → با AudD تشخیص می‌ده (اسم آهنگ،
  خواننده، لینک اسپاتیفای/اپل‌میوزیک).
- «خلاصه گروه» → آخرین پیام‌های متنیِ گروه (فقط تو حافظه‌ی سشن، نه دیتابیس) رو
  با هوش مصنوعی خلاصه می‌کنه.

## کلیدهای لازم رو Railway → Variables

اینا رو که برام فرستادی رو Railway ست کن (نه تو کد):

```
TMDB_API_KEY=...
AUDD_API_TOKEN=...
```

`OPENROUTER_API_KEY` که از قبل داری همینجا هم استفاده می‌شه (لازم نیست دوباره
بذاری).

⚠️ چون این کلیدها یه‌بار پلین تو چت رد و بدل شدن، بهتره از پنل AudD و TMDB
رجنریتشون کنی (کلید قدیمی رو باطل کن، جدید بساز) و همون جدیده رو رو Railway بذار.

`TWELVE_LABS_API_KEY` رو فعلاً تو کد استفاده نکردم — تشخیص فیلم از ویدیو با
گرفتن یه فریم + مدل ویژن انجام می‌شه که ساده‌تره؛ اگه بعداً خواستی دقت تشخیص
از رو ویدیو (نه فقط عکس) بالاتر بره، Twelve Labs گزینه‌ی بعدیه.

## نیازمندی جدید
`requirements.txt` آپدیت شد: `python-telegram-bot[job-queue]` (برای زمان‌بندی
یادآورها لازمه؛ اگه از قبل کار می‌کرد چیزی خراب نمی‌شه، فقط مطمئن‌تر شد).

## نکته درباره‌ی ساعت گاتهام
تو پیام قبلیت گفتی ساعت اشتباهه، ولی از رو اسکرین‌شات خودت متن نمایش‌داده‌شده
درست بود (وقت تهران، تاریخ شمسی/قمری/میلادی هماهنگ). اگه هنوز جایی غلط
می‌بینی، دقیق بگو کجا (کدوم دکمه/پیام) تا رفعش کنم.
